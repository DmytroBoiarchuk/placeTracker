import classes from "./SavedPlaces.module.scss"

const SavedPlaces = () => {
    return (
        <div className={classes.savedPlacesButton}>
            Saved
        </div>
    );
};

export default SavedPlaces;